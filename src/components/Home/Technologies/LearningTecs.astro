---
import TecsContent from "./components/TecsContent.astro"
import { Icons } from "./TechsIcons/LearningIcons"
const icons = Icons.Learning
---

<TecsContent>
  {
    icons.map((icon) => (
      <a href={icon.page} target={"_blank"}>
        <div>
          <icon.icon />
          <span>{icon.name}</span>
        </div>
      </a>
    ))
  }
</TecsContent>
